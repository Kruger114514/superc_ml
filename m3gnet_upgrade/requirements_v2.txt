# v2 (M3GNet) 精简依赖
# ==================
# 修正版: 去掉 matminer (v2 用 pymatgen 不用 matminer)
# Python 版本: 3.10 或 3.11 都可以

# --- 核心: PyTorch + matgl ---
# CPU 版本 (本地调试用), GPU 版本上云时再装
torch>=2.2.0,<2.5.0
# matgl 是 M3GNet 的 PyTorch 实现, 必备
matgl>=1.1.0,<2.0.0

# --- 数据处理 ---
pymatgen>=2024.1.1
ase>=3.22.1
numpy<2
pandas>=2.0

# --- 训练 (拿到 GPU 后用) ---
lightning>=2.2.0
tensorboard>=2.15

# --- 评估 + 可视化 ---
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4

# --- 数据集下载 ---
# datasets: HuggingFace 用 (HTSC-2025)
# jarvis-tools: JARVIS-DFT 用 (如果之后想试)
# 这两个**可选**, SuperBand 用 git clone 不需要这俩
# datasets>=2.14
# jarvis-tools>=2024.1.1

# --- v2 Web UI ---
streamlit>=1.30
joblib>=1.3

# --- 其他工具 ---
PyYAML>=6.0
tqdm>=4.66
